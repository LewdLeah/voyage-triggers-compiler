export const meta = {
  conditions: [{ type: 'party-location', operator: 'equals', value: 'Temple of Light' }],
  effects: [{ type: 'story', instruction: 'The temple bell tolls in welcome.' }],
  recurring: false
};
